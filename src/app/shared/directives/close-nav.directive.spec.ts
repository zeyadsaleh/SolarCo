import { CloseNavDirective } from './close-nav.directive';

describe('CloseNavDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseNavDirective();
    expect(directive).toBeTruthy();
  });
});
